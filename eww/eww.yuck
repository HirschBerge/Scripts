(deflisten notifs :initial ""
  `~/.config/eww/main.py`)

(defwindow notifications
           :monitor 1
           :geometry (geometry :x "20px"
                               :y "20px"
                               :anchor "top right")
           :stacking "overlay"
           (literal :content notifs))
